import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center">
      <svg className="animate-spin h-10 w-10 border-4 border-l-pink-600 rounded-full" />
    </div>
  );
};

export default Loading;
